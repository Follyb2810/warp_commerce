import React from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";

const OrderCard = ({ order }) => {
  return (

    // <Card className="p-4 mb-4 shadow-md rounded-lg">
    //   <CardContent>
    //     <div className="flex justify-between items-center mb-2">
    //       <h3 className="text-lg font-semibold">Order ID: {order._id}</h3>
    //       <Button
    //         onClick={() => {}}
    //         className="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded"
    //       >
    //         {order.status}
    //       </Button>
    //     </div>
    //     <p className="text-sm text-gray-600">Total Amount: ${order.totalAmount}</p>
    //     <p className="text-sm text-gray-600">Buyer: {order.buyer}</p>
    //     <p className="text-sm text-gray-600">Seller: {order.seller}</p>
    //     <p className="text-sm text-gray-600">Transaction Hash: {order.payment.txHash}</p>
    //     <p className="text-sm text-gray-600">Created At: {new Date(order.createdAt).toLocaleString()}</p>
    //   </CardContent>
    // </Card>
    <h1>ello</h1>
  );
};

export default OrderCard;
